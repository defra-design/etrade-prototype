module.exports = function(router) {
  // Load helper functions
  // ADD extra routing here if needed.
  // require('./start-page.js')(router)


  // router.post('/beta/verify/enter-code', function(req, res) {
  //   if (req.session.data.allow_continue!="no" && req.session.data.journey == "returning" || (req.session.data.allow_continue!="no" && req.session.data.journey != "returning" && req.session.data.email && req.session.data.email == req.session.data.savedEmail)) {
  //     page = 'import-transport-how-to'
  //     res.redirect(301, '/gforms/verify/continue-form');
  //   } else {
  //     req.session.data.savedEmail = req.session.data.email
  //     res.redirect(301, '/gforms/verify/email-confirmed');
  //
  //   }
  // })




}
